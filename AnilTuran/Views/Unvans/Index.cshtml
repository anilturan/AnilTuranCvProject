@model IEnumerable<AnilTuran.Models.Unvan>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UnvanAdi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserInformationID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UnvanAdi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserInformationID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UnvanID }) |
            @Html.ActionLink("Details", "Details", new { id=item.UnvanID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UnvanID })
        </td>
    </tr>
}

</table>
